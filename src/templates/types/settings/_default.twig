{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Dropdown Label"|t('listingsource'),
    instructions: "Enter the label you want to appear in the link dropdown."|t('listingsource'),
    id: type.type|id~'-customLabel',
    name: 'customLabel',
    value: type.customLabel ?? '',
    placeholder: type.defaultLabel,
    errors: type.getErrors('customLabel')
}) }}
